# ================================================
# 默认站点 - PHP 8.3
# 根据项目需要复制此文件并修改 upstream 指向
# 例如使用 PHP 7.4: fastcgi_pass php74;
#      使用 PHP 8.4: fastcgi_pass php84;
# ================================================
server {
    listen       80;
    server_name  localhost;

    root   /var/www/html;
    index  index.php index.html index.htm;

    access_log  /var/log/nginx/default-access.log  main;
    error_log   /var/log/nginx/default-error.log   warn;

    # 静态文件缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 30d;
        add_header Cache-Control "public, no-transform";
    }

    # PHP 处理（使用 php83 upstream）
    location ~ \.php$ {
        try_files      $uri =404;
        fastcgi_pass   php83;
        fastcgi_index  index.php;
        fastcgi_param  SCRIPT_FILENAME $document_root$fastcgi_script_name;
        include        fastcgi_params;
        fastcgi_read_timeout 300;
        fastcgi_buffer_size 128k;
        fastcgi_buffers 4 256k;
    }

    # Laravel / 单入口框架支持
    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    # 禁止访问隐藏文件
    location ~ /\. {
        deny all;
    }
}

# ================================================
# 示例：使用 PHP 7.4 的项目站点（取消注释使用）
# ================================================
# server {
#     listen 80;
#     server_name php74.local;
#     root /var/www/html/your-php74-project/public;
#     index index.php index.html;
#
#     location / {
#         try_files $uri $uri/ /index.php?$query_string;
#     }
#
#     location ~ \.php$ {
#         try_files $uri =404;
#         fastcgi_pass php74;
#         fastcgi_index index.php;
#         fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
#         include fastcgi_params;
#     }
# }

# ================================================
# 示例：使用 PHP 8.4 的项目站点（取消注释使用）
# ================================================
# server {
#     listen 80;
#     server_name php84.local;
#     root /var/www/html/your-php84-project/public;
#     index index.php index.html;
#
#     location / {
#         try_files $uri $uri/ /index.php?$query_string;
#     }
#
#     location ~ \.php$ {
#         try_files $uri =404;
#         fastcgi_pass php84;
#         fastcgi_index index.php;
#         fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
#         include fastcgi_params;
#     }
# }
